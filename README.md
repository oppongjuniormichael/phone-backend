# Phone Backend

Standalone Node.js + Express API for the phone shop frontend.

## Run

```bash
npm install
npm run prisma:generate
npm run prisma:migrate
npm run prisma:seed
npm run dev
```

The API listens on `http://localhost:4000` by default.